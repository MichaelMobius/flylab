export const LIMITS={neurons:262144,edges:20000000,bytes:64*1024*1024};
export function header(buf,magic,words){if(!(buf instanceof ArrayBuffer)||buf.byteLength<words*4+5)throw new Error('Truncated packed file');const v=new DataView(buf);if(v.getUint32(0,true)!==magic||v.getUint32(4,true)!==1)throw new Error('Unsupported packed file');const h=Array.from({length:words},(_,i)=>v.getUint32(i*4,true));if(!h[2]||h[2]>LIMITS.neurons)throw new Error('Neuron count outside budget');if(words===5&&(h[3]>LIMITS.edges||h[4]>65535))throw new Error('Graph dimensions outside budget');return h;}
export function validateMetadata(data){if(!Array.isArray(data.meta.types)||data.meta.types.length!==data.N)throw new Error('Neuron type metadata mismatch');for(const s of data.bodymap?.sensors||[])for(const i of s.idx||[])if(!Number.isInteger(i)||i<0||i>=data.N)throw new Error('Invalid bodymap index');for(const i of data.bodymap?.feeding||[])if(!Number.isInteger(i)||i<0||i>=data.N)throw new Error('Invalid feeding index');}
